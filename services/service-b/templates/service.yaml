apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.appName }}
spec:
  ports:
  - port: 81
    targetPort: 81
    nodePort: 30081
    protocol: TCP
    name: "service-b"
  selector:
    app: {{ .Values.appName }}
    tier: backend
  type: NodePort